<!DOCTYPE html>
    <html>
        <body>
            <head></head>
                <h1>Users</h1>
                    <% @users.each do |user| %>
                        <li><%= link_to "#{user.userName}", user %> </li>
                        <br>
                    <% end %>
                </ul>
        </body>
    </html>